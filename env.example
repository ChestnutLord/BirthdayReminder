# Скопируйте в .env и подставьте свои значения.
# В .env каждая переменная должна быть на отдельной строке.

# База данных (для docker-compose)
POSTGRES_DB=birthday_reminder
POSTGRES_USER=birthday_user
POSTGRES_PASSWORD=your_postgres_password

# Spring DataSource (приложение подключается к контейнеру postgres)
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/birthday_reminder
SPRING_DATASOURCE_USERNAME=birthday_user
SPRING_DATASOURCE_PASSWORD=your_postgres_password

# Рассылка напоминаний (адреса через запятую; реальная отправка не выполняется)
NOTIFICATION_RECIPIENTS=your_email@gmail.com
